# 孙颖莎传记网站

这是一个关于乒乓球运动员孙颖莎的传记网站，包含视频粒子特效和问答游戏功能。

## 功能特性

- 🎬 视频粒子特效（基于 Three.js 和 MediaPipe）
- 🎮 视频问答游戏
- 📱 响应式设计
- 🎨 现代化 UI 界面

## 技术栈

- **框架**: Next.js 14
- **UI**: React 18, Tailwind CSS
- **3D 图形**: Three.js
- **视频处理**: MediaPipe Selfie Segmentation
- **动画**: Framer Motion, GSAP

## 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start
```

## GitHub Pages 部署

项目已配置 GitHub Actions 自动部署到 GitHub Pages。

### 部署步骤

1. **启用 GitHub Pages**：
   - 进入仓库的 Settings → Pages
   - Source 选择 "GitHub Actions"

2. **推送代码**：
   - 将代码推送到 `main` 分支
   - GitHub Actions 会自动构建并部署

3. **访问网站**：
   - 部署完成后，访问 `https://<你的用户名>.github.io/<仓库名>/`

### 手动触发部署

如果需要手动触发部署，可以在 Actions 标签页中点击 "Deploy to GitHub Pages" workflow，然后点击 "Run workflow"。

## 项目结构

```
├── app/              # Next.js App Router 页面
│   ├── game/        # 问答游戏页面
│   └── ...
├── components/       # React 组件
│   └── ShaderVideoParticle.tsx  # 视频粒子特效组件
├── data/            # 数据文件
│   └── quiz-questions.ts  # 问答游戏数据
├── public/          # 静态资源
│   └── videos/      # 视频文件
└── types/           # TypeScript 类型定义
```

## 注意事项

- 视频文件较大，建议使用 Git LFS 或外部存储
- MediaPipe 需要从 CDN 加载模型文件，确保网络连接正常
- GitHub Pages 使用静态导出，确保所有功能在静态模式下正常工作

## 许可证

MIT
